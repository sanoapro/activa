# ── .NET / F# ────────────────────────────────────────────────────────────────
bin/
obj/
*.fable-temp.csproj
*.user

# ── Fable ────────────────────────────────────────────────────────────────────
# La salida de scripts/ SÍ se versiona: así el repositorio se puede publicar en
# GitHub Pages sin CI y la carpeta funciona de forma independiente. Solo se
# ignora la metadata interna del compilador.
scripts/fable_modules/project_cracked.json

# ── Sistema ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
node_modules/
_site/
_probe.html
